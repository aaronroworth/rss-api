'use strict'
module.exports = function (request) {
  var parser = require('rss-parser')

return "blah"//parser.parseURL('http://blinkmobile.com.au/BlinkMobileDeveloperBlogRSSFeed?format=feed', function(err, parsed) {
		//"blah" + JSON.stringify(parsed.feed)
		
	//})
}
